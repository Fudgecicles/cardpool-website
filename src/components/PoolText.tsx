import styled from "styled-components";
import Colors from "../config/Colors";

const PoolStyled = styled.span`
  z-index: 2;
  color: ${Colors.pool};
  font-weight: bold;
`;

export default PoolStyled;
